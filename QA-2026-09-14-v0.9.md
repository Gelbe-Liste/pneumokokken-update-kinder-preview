# QA – pneumokokken-update-kinder v0.9 – 14.09.2026

## Basis

- v0.9 wurde direkt aus v0.7 erstellt.
- v0.8 wurde nicht als Basis verwendet.
- `src/styles.css` wurde in v0.9 nicht verändert.
- Bestehende Farbwelt, Kapitel-Layout, Hintergründe und Fachgrafiken aus v0.7 bleiben erhalten.

## Inhaltliche Checks

- [x] 14 Kapitel vorhanden.
- [x] Standardimpfung: PCV13/PCV15 korrekt als STIKO-Optionen eingeordnet.
- [x] Indikationsimpfung: PCV20 auf STIKO-Risikokinder 2–17 Jahre begrenzt.
- [x] Keine Adult-Daten in `src/project.js` aufgenommen.
- [x] Keine direkten PCV20-vs.-PCV15-VE-, Kosten- oder Replacement-Überlegenheitsclaims übernommen.
- [x] Aktuelle Impfquoten/Versorgungslücken ergänzt.
- [x] Theoretische Serotypenabdeckung ausdrücklich von klinischer Wirksamkeit abgegrenzt.
- [x] Rechtehinweis für Referenzlabor-Daten ergänzt.
- [x] Sponsoring-/Unabhängigkeitshinweis im Web-Impressum und in der PDF-Engine ergänzt.

## Technische Checks

- [x] `src/project.js` per Node importiert: 14 Seiten / 12 Quellen / contentVersion v0.9.
- [x] Alle in `project.js` referenzierten lokalen Bild- und Hintergrund-Assets vorhanden.
- [x] `package.json` und `package-lock.json` valide JSON; Version auf `0.9.0` gesetzt.
- [x] `src/pdf/generatePdf.js` per `node --check` ohne Syntaxfehler.
- [ ] Vollständiger Vite-Build in dieser Umgebung: nicht abschließend möglich, da `npm ci` beim Zugriff auf die npm-Registry in ein Timeout lief und keine lokale npm-Cache-Basis vorhanden war.

## Vor Go-live

1. `npm ci && npm run build` in GitHub/Vercel oder lokaler Entwicklungsumgebung ausführen.
2. Browser-QA Desktop/iOS/Android durchführen.
3. Referenzlabor-Nutzungsfreigabe klären.
4. Sponsorlogos / Pflichttexte final mit Medical/Compliance abstimmen.
5. Finale CME-URL und CME-Punkte einsetzen.
